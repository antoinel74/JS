let n = parseInt(prompt("Enter a number :"));
let sum = 0;

// Step 3: Use a for loop to collect n numbers and calculate the sum 

for (let i = 1; i <= n; i++) {
    let userInput = parseInt(prompt("Enter number :"));
    sum += userInput;
  }

  alert("The sum of your numbers are " + sum);


